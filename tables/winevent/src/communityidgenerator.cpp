
#include "communityidgenerator.h"

namespace zeek {

  bool checkProtocol(std::string proto) {
    //bool exists = std::find(std::begin(commID_proto),
    //                        std::end(commID_proto),
    //                        proto) != std::end(commID_proto);
    bool exists = false;
    return exists;
  }

} // namespace zeek
